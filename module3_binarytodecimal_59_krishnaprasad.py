#python program to convert binary to decimal
binary=input("enter the number in binary form:")

if binary=='x':
    exit();
else:
    decimal=int(binary,2);
    print(binary,"in decimal=",decimal);
